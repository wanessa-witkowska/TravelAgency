<UserControl x:Class="TravelAgency.Views.AddCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TravelAgency.Views;component/SharedResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- Buttons -->
        <Button Content="Back" Grid.Row="0" Grid.Column="0" Command="{Binding BackCommand}" Margin="5,5,5,0" Width="100" HorizontalAlignment="Left" Style="{StaticResource PrimaryButton}" Tag="&#xE72B;" />
        <Button Content="Save" Grid.Row="0" Grid.Column="1" Command="{Binding SaveCommand}" Margin="5,5,5,0" Width="100" HorizontalAlignment="Right" Style="{StaticResource PrimaryButton}" Tag="&#xE74E;" />
        <!-- Form -->
        <GroupBox Header="Customer Data" Grid.Row="1" Grid.ColumnSpan="2" Margin="5" Padding="10">
            <StackPanel>
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="First Name:" Style="{StaticResource PrimaryTextBlock}" Width="100"/>
                    <TextBox Text="{Binding FirstName, ValidatesOnDataErrors=True}" Style="{StaticResource PrimaryTextBox}" Width="200"/>
                </WrapPanel>
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Last Name:" Style="{StaticResource PrimaryTextBlock}" Width="100"/>
                    <TextBox Text="{Binding LastName, ValidatesOnDataErrors=True}" Style="{StaticResource PrimaryTextBox}" Width="200"/>
                </WrapPanel>
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Email:" Style="{StaticResource PrimaryTextBlock}" Width="100"/>
                    <TextBox Text="{Binding Email, ValidatesOnDataErrors=True}" Style="{StaticResource PrimaryTextBox}" Width="200"/>
                </WrapPanel>
                <WrapPanel Margin="0,0,0,5">
                    <TextBlock Text="Phone:" Style="{StaticResource PrimaryTextBlock}" Width="100"/>
                    <TextBox Text="{Binding PhoneNumber, ValidatesOnDataErrors=True}" Style="{StaticResource PrimaryTextBox}" Width="200"/>
                </WrapPanel>
            </StackPanel>
        </GroupBox>
        <!-- Response message -->
        <TextBlock Text="{Binding Response}" Grid.Row="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,0,0,5"/>
    </Grid>
</UserControl>
